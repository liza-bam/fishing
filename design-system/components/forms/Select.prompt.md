Labelled dropdown matching Input. Pass `options` as strings or `{value,label}`.

```jsx
<Select label="Party size" options={["1–2 guests","3–4 guests","5+ guests"]} />
<Select label="Route" options={[{value:'w',label:'Western'},{value:'s',label:'Southern'}]} onDark />
```
