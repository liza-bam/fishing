Labelled text field with uppercase overline label and focus ring in lagoon.

```jsx
<Input label="Full name" placeholder="Jane Darwin" required />
<Input label="Email" type="email" hint="We'll only use this to reply." />
```
Set `onDark` when placing on a deep-sea section.
