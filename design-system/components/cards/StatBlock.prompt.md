Large serif figure + caption. Compose several in a row for an impact band.

```jsx
<StatBlock value="3" label="headline species — marlin, tuna, wahoo" />
<StatBlock value="12mo" label="of year-round offshore fishing" onDark />
```
Set `onDark` over deep-sea sections; `align="center"` for centered stat rows.
